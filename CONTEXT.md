# kclaw

单守护进程的个人 AI 助理：`@kclaw/core` 引擎 + Fastify daemon + CLI/Web 双客户端，状态以会话事件溯源持久化。

## Language

### 术语与文风

技术文档用词三原则：

1. 机翻腔的词，要么换成通俗中文，要么直接用英文原词（hook / budget / provider / fallback）。
2. 自造的隐喻词，不用它，把它代表的具体含义直接写出来（"待遇派生"→"引擎从 risk 与参数 schema 推导该工具的处理方式"）。
3. 只反映当前代码，不写演化过程（"旧版如何、现在如何""设计教训"），没人关注之前是什么；兼容行为除外（"旧 `config.yaml` 仍被读取"是当前行为，照写）。

已清除的词及替代（写新文档时同样回避）：落盘→存储、缺省→默认、消费→处理、水位/水线→阈值线、内化→沉淀、记忆塔→记忆系统、点名→指定、回落→回退、兜底→保底、收口→结束、真相源/唯一真相→唯一权威数据/以 X 为准、缝合层→resolveConfirmation、台账/账本→记录/表、起转→启动、退役→废弃、让位→停用、招募→添加、孤儿→残留、竞速→race、哨兵→断言、信封→帧、接缝→接入点、底座→基础库、死线→上限、响亮→明确、留痕→记录、旅程→流转、探测→检测、纵深→多层、剥离→清洗、待遇→处理方式、收窄→具体化（可见范围缩小/限制在）、容忍→兼容、推翻→与 X 不一致、收束→自动转 inactive、脉络→背景、排他→具体化（严格晚于）、门面→删词、包裹/遮蔽/手编/向导/价格表/桶/定稿/招募→具体化、少用破折号（解释用冒号或括号，不用"——X——"插入语）。



### 指令的四个来源

**Skill（技能）**:
一份含元数据头与指令正文的 SKILL.md 能力包；模型可自主发现并按需加载正文。可见性可配置。
_Avoid_: 插件、扩展、宏

**Command（自定义命令）**:
用户显式敲 `/xxx` 才触发的提示词模板（`~/.kclaw/commands/*.md`）；模型不能自主发现它。
_Avoid_: skill、斜杠脚本

### 技能复用

**Skill Reuse（技能复用）**:
把其他 coding agent 已有的技能目录以软链接接入 kclaw，内容不复制，仍由源目录维护。
_Avoid_: 导入、安装、同步

**Discovery（检测）**:
扫描其他 coding agent 的用户级技能目录，找出其中可复用的技能。
_Avoid_: 扫描、导入 wizard

**Visibility Tier（可见档位）**:
一个技能在用户面（指定、技能页）与模型面（系统提示词技能列表）的呈现档位，共四档：完全可见、仅用户、仅模型、暂不启用。自有技能与复用技能共用这套档位。
_Avoid_: 权限、启用开关

**Cognition（认知）**:
关于用户与项目的常驻事实性记忆（persona/rule/wiki），注入系统提示词，内容是"是什么"而非"怎么做"。
_Avoid_: 人设文件、知识库

**Persona（人设）**:
`~/.kclaw/AGENTS.md`，整份常驻系统提示词的全局身份与规则。
_Avoid_: 系统配置

### 运行与中断

**Run（一轮）**:
由一条用户消息（或定时任务、subagent 调用）触发、到模型给出终结回应为止的完整执行过程，是停止、计量和审计的基本单位。
_Avoid_: 回合、请求

**Step（一步）**:
一次模型调用及其引发的全部工具执行——工具的来回算在同一 Step 里，不算新的一步；一轮 Run 由零或多个 Step 组成。对应代码里的循环迭代（配置键 maxIterations 保持原名）。
_Avoid_: 迭代（行文里不再用"迭代"指称 Step）、轮次

**Turn**:
Run 的英文行话别名，跨仓库讨论时对齐用（一次输入引发的完整流程）；指一次模型调用时永远说 Step。
_Avoid_: 用 Turn 指 Step（openclaw 的 maxTurns、OpenAI Agents SDK 的 max_turns 实际指的都是 Step）

**Note（批注）与载具消息**:
批注是系统（而非用户或模型）写入对话的信息块，共六种来源（见 blocks 参考）。批注总是"搭车"：持久化时寄居在一条**载具消息**的块列表里，给模型看时以 `<system-reminder>` 标签拼进载具内容——它与载具内容无关，载具只是运输工具。
_Avoid_: 把批注当独立消息；说"挂载"时想成独立通道（它没有自己的通道）

**Stop（停止）**:
不附带新消息、立即终止当前一轮运行的动作；已生成的部分回复作为普通消息保留，并标记“已中断”。
_Avoid_: 打断、中止、cancel

**Aborted（已中断）**:
一轮运行被停止后的落停结果；半途产出的回复照常留存，只是带此标记。
_Avoid_: 失败、取消

**Disposition（排队处置）**:
当前一轮正在运行时，新发的消息按处置决定去向，共三种。

**Steer（引导）**:
把消息作为补充递给正在回答的模型，本轮回答会把它纳入考虑，不终止运行。

**Wait（等待）**:
消息排进队尾，等当前一轮结束后作为新问题开跑。

**Interrupt（中断）**:
发送新消息的同时终止当前一轮，这条消息随即成为下一轮的开始。
_Avoid_: 把“中断”当纯停止的含义用——那个词是“停止”

### 编辑重试

**Edit & Retry（编辑重试）**:
改写最后一条用户消息并从那里重新发起一轮；原消息及其之后的全部回复从对话视图隐去，事件流里完整保留。
_Avoid_: 重发、撤回

**Regenerate（重新生成）**:
不改文本，原样重跑最后一条用户消息；与编辑重试同一机制、另一个入口。

**Truncation（截断）**:
宣告“从最后一条用户消息起的内容不再属于对话视图”的动作；事件流只追加标记，已发生的历史一字不删。
_Avoid_: 删除（物理删除恰恰不会发生）

**Idle（会话空闲）**:
没有正在运行的轮次、没有排队消息、也没有进行中压缩的状态；编辑重试与重新生成只在此状态开放。

### 团队（Agent Team）

**Lead（组长）**:
一个会话里的主 agent 对其团队而言的身份。每个会话天生是自己团队的组长，无需创建动作。
_Avoid_: 队长、协调者

**Teammate（组员）**:
被组长招进成员名单、有名字、常驻待命的子会话。与 subagent 的区别：持续存在、可反复派活、可互发消息。
_Avoid_: 团队成员、成员

**Member List（成员名单）**:
组长记在案的全部团队成员名单，从第一个成员入册才开始存在。
_Avoid_: 花名册、Roster、成员列表

**Mailbox（收信箱）**:
团队内传递消息的持久化通道：消息先持久化、对方确认收下才标记送达、崩溃后按未送达记录重放补投。
_Avoid_: 邮箱、消息队列、收件箱（指 UI 呈现时可用）

**Board（任务板）**:
团队共享的任务看板：任务是带状态、负责人、依赖的完整快照，落在团队目录的 `task/` 下。
_Avoid_: 看板系统、任务列表（指 Claude Code 语境时除外）

**Attempt（执行尝试）**:
任务的第 N 次执行，带唯一标识；改派或重试作废旧尝试后，旧执行者迟到的更新无法覆盖新结果。
_Avoid_: 重试次数

**Team Directory（团队目录）**:
一支团队的全部持久状态所在的工作区目录：`.kclaw/teams/<队名>/` 放团队记录、成员名单与收信箱，`.kclaw/tasks/<队名>/` 放任务快照与执行锁。
_Avoid_: 状态目录、TeamStore

### 界面

**File Mention（文件指定）**:
在输入框用 `@相对路径` 指定会话工作区内的一个文件：消息照原文收发与留存，模型收到的是末尾多出一行"用 fs_read 读取该文件"的指示。联想抽屉只列 git 未忽略的文件。
_Avoid_: 文件引用、附件（附件是上传副本，指定是就地指向）

**Audit（审计）**:
会话事件流（会话的唯一权威数据）在 Web 端的只读回看视图，原则是"让用户掌握发生的一切"。
_Avoid_: 轨迹、trail、trajectory、日志页

**Subagent（subagent）**:
主对话模型经 `subagent_run` 工具自主派出的短命执行单元：独立子会话（`meta.parentSessionId` 标识）、单层委派、结题答复即工具结果。子会话对用户只读。
_Avoid_: 子agent、子代理、平行代理、分支会话

### 压缩

**Waterline（阈值线）**:
压缩触发的五条阈值线，每条是上下文 budget 的百分比，由阈值线模块统一解析为绝对 token 阈值：省略线（默认 0.70，请求组装时旧工具输出的省略 budget）、预压线（0.75，聊天途中后台预压缩的起点）、黄线（0.80，收尾压缩的触发线）、红线（0.90，聊天途中紧急压缩）、目标线（0.33，压缩后保留部分的目标占比）。
_Avoid_: 压缩阈值、watermarks

### 架构约定

**Protocol（线上形状）**:
消息、事件、指令帧、排队条目的唯一类型出处：`@kclaw/core/protocol` 纯类型出口。server / web / cli 三端一律引用，不手抄镜像。
_Avoid_: mirrors 注释、各端自定义 wire 类型
